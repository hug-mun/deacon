# This project uses the public npm registry. The machine-global ~/.npmrc points
# at a private AWS CodeArtifact registry from a different account; this file
# overrides it for everything inside this repo.
registry=https://registry.npmjs.org/
